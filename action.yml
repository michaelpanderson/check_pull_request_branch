name: "Check pull"
description: Check pull
author:
branding:
  icon: 'terminal'
  color: 'gray-dark'
runs:
  using:  "composite"
  steps:
    - uses: actions/checkout@v2
    - run: ${{ github.action_path }}/check_pr.sh
      shell: bash
